{
    "devDependencies": {
        "concurrently": "^3.5.0",
        "lerna": "^2.0.0"
    },
    "scripts": {
        "client": "cd packages/client && npm run start",
        "dev": "concurrently \"cd packages/server && npm run server\" \"cd packages/client && npm run start\""
    }
}
